# Verified against the versions this project was built and tested with.

# Core service
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5

# Document parsing
pypdf==6.18.0
python-docx==1.2.0

# Answer generation (optional at runtime: without credentials the service
# falls back to extractive answers)
anthropic==1.4.0
